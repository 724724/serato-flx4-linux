diff --git a/dlls/mmdevapi/client.c b/dlls/mmdevapi/client.c
index d29969b..947ef88 100644
--- a/dlls/mmdevapi/client.c
+++ b/dlls/mmdevapi/client.c
@@ -163,9 +163,13 @@ static HRESULT adjust_timing(struct audio_client *client, const BOOLEAN force_de
                 if (*duration != *period)
                     return AUDCLNT_E_BUFDURATION_PERIOD_NOT_EQUAL;
 
-                FIXME("EXCLUSIVE mode with EVENTCALLBACK\n");
+                FIXME("EXCLUSIVE mode with EVENTCALLBACK: emulating with timer-driven stream\n");
 
-                return AUDCLNT_E_DEVICE_IN_USE;
+                /* Validate the WASAPI-mandated duration == period above, then
+                 * enlarge Wine's timer-driven emulation buffer. A one-period
+                 * buffer leaves no scheduling margin for the application
+                 * callback and produces audible underruns. */
+                *duration = 8 * *period;
             } else if (*duration < 8 * *period)
                 *duration = 8 * *period; /* May grow above 2s. */
         }
diff --git a/dlls/winealsa.drv/alsa.c b/dlls/winealsa.drv/alsa.c
index 046b447..e37a82e 100644
--- a/dlls/winealsa.drv/alsa.c
+++ b/dlls/winealsa.drv/alsa.c
@@ -882,13 +882,14 @@ static NTSTATUS alsa_create_stream(void *args)
     stream->mmdev_period_frames = muldiv(params->fmt->nSamplesPerSec,
             stream->mmdev_period_rt, 10000000);
 
-    /* Buffer 4 ALSA periods if large enough, else 4 mmdevapi periods */
-    stream->alsa_bufsize_frames = stream->mmdev_period_frames * 4;
+    /* Buffer 8 ALSA periods if large enough, else 8 mmdevapi periods
+     * (upstream uses 4; doubled for underrun headroom on USB devices) */
+    stream->alsa_bufsize_frames = stream->mmdev_period_frames * 8;
     if(err < 0 || alsa_period_us < params->period / 10)
         err = snd_pcm_hw_params_set_buffer_size_near(stream->pcm_handle,
                 stream->hw_params, &stream->alsa_bufsize_frames);
     else{
-        unsigned int periods = 4;
+        unsigned int periods = 8;
         err = snd_pcm_hw_params_set_periods_near(stream->pcm_handle, stream->hw_params, &periods, NULL);
     }
     if(err < 0)
@@ -1635,6 +1636,12 @@ static NTSTATUS alsa_timer_loop(void *args)
     struct alsa_stream *stream = handle_get_stream(params->stream);
     LARGE_INTEGER delay, next;
     int adjust;
+    struct sched_param sp = { .sched_priority = 10 };
+
+    /* This pump must run every period or the ALSA buffer underruns; plain
+     * SCHED_OTHER gets preempted under desktop load. Best effort — fails
+     * silently without RLIMIT_RTPRIO (realtime group). */
+    pthread_setschedparam(pthread_self(), SCHED_FIFO, &sp);
 
     alsa_lock(stream);
 
@@ -2102,6 +2109,52 @@ static NTSTATUS alsa_get_mix_format(void *args)
         goto exit;
     }
 
+    if(max_channels > 6){
+        /* The endpoint is a plug device whose channel count is unconstrained
+         * (reported as 10000), so the stereo fallback above hides the real
+         * channel count of multichannel interfaces (e.g. 4ch DJ controllers).
+         * Query the underlying raw hw device instead. The plug handle holds
+         * the hw substream, so close it first. */
+        unsigned int card, dev, hw_max = 0;
+
+        free(hw_params);
+        hw_params = NULL;
+        snd_pcm_close(pcm_handle);
+        pcm_handle = NULL;
+
+        if(sscanf(params->device, "plughw:%u,%u", &card, &dev) == 2){
+            char hwname[64];
+            snd_pcm_t *hw_pcm;
+            char *card_name = NULL;
+            snd_pcm_hw_params_t *hw_hp;
+
+            sprintf(hwname, "hw:%u,%u", card, dev);
+            if(SUCCEEDED(alsa_open_device(hwname, params->flow, &hw_pcm, &hw_hp))){
+                if(snd_pcm_hw_params_any(hw_pcm, hw_hp) >= 0 &&
+                   snd_pcm_hw_params_get_channels_max(hw_hp, &hw_max) >= 0 &&
+                   hw_max > 2 && hw_max <= 8 && !(hw_max & 1)){
+                    fmt->Format.nChannels = hw_max;
+                    fmt->dwChannelMask = get_channel_mask(hw_max);
+                }
+
+                /* Serato's FLX4 backend renders signed 32-bit PCM samples,
+                 * but advertising FLOAT from plughw makes those bit patterns
+                 * get converted as floats, saturating the USB output. */
+                if(params->flow == eRender &&
+                   snd_pcm_format_mask_test(formats, SND_PCM_FORMAT_S32_LE) &&
+                   snd_card_get_name(card, &card_name) >= 0 && card_name &&
+                   !strcmp(card_name, "DDJ-FLX4")){
+                    fmt->Format.wBitsPerSample = 32;
+                    fmt->SubFormat = KSDATAFORMAT_SUBTYPE_PCM;
+                }
+
+                free(hw_hp);
+                snd_pcm_close(hw_pcm);
+            }
+            free(card_name);
+        }
+    }
+
     fmt->Format.nBlockAlign = (fmt->Format.wBitsPerSample * fmt->Format.nChannels) / 8;
     fmt->Format.nAvgBytesPerSec = fmt->Format.nSamplesPerSec * fmt->Format.nBlockAlign;
 
@@ -2111,7 +2164,8 @@ static NTSTATUS alsa_get_mix_format(void *args)
 exit:
     free(formats);
     free(hw_params);
-    snd_pcm_close(pcm_handle);
+    if(pcm_handle)
+        snd_pcm_close(pcm_handle);
 
     return STATUS_SUCCESS;
 }
@@ -2268,6 +2322,14 @@ static NTSTATUS alsa_set_volumes(void *args)
     struct alsa_stream *stream = handle_get_stream(params->stream);
     unsigned int i;
 
+    /* Exclusive-mode streams bypass the audio engine on Windows, so session/
+     * stream volumes are not applied to the samples there. Applying them here
+     * as instantaneous gain steps (no ramp) produces a click per change —
+     * apps that ride the volume (e.g. limiters in DJ software) turn that
+     * into continuous zipper noise. Keep exclusive streams bit-exact. */
+    if (stream->share == AUDCLNT_SHAREMODE_EXCLUSIVE)
+        return STATUS_SUCCESS;
+
     for(i = 0; i < stream->fmt->nChannels; i++)
         stream->vols[i] = params->volumes[i] * params->session_volumes[i] * params->master_volume;
 
diff --git a/dlls/winealsa.drv/alsamidi.c b/dlls/winealsa.drv/alsamidi.c
index fbc65bd..3d70d74 100644
--- a/dlls/winealsa.drv/alsamidi.c
+++ b/dlls/winealsa.drv/alsamidi.c
@@ -49,6 +49,8 @@
 
 #include "unixlib.h"
 
+static int match_flx4(const WCHAR *s);
+
 WINE_DEFAULT_DEBUG_CHANNEL(midi);
 
 struct midi_dest
@@ -920,6 +922,12 @@ static UINT midi_out_get_devcaps(WORD dev_id, MIDIOUTCAPSW *caps, UINT size)
 
     memcpy(caps, &dests[dev_id].caps, min(size, sizeof(*caps)));
 
+    if (match_flx4(dests[dev_id].caps.szPname) && size >= sizeof(WORD)*2) {
+        caps->wMid = 0x2b73;
+        caps->wPid = 0x0045;
+        fprintf(stderr, "[FLX4PATCH] OUT devcaps wMid/wPid injected dev_id=%u\n", dev_id);
+    }
+
     return MMSYSERR_NOERROR;
 }
 
@@ -1323,6 +1331,12 @@ static UINT midi_in_get_devcaps(WORD dev_id, MIDIINCAPSW *caps, UINT size)
 
     memcpy(caps, &srcs[dev_id].caps, min(size, sizeof(*caps)));
 
+    if (match_flx4(srcs[dev_id].caps.szPname) && size >= sizeof(WORD)*2) {
+        caps->wMid = 0x2b73;
+        caps->wPid = 0x0045;
+        fprintf(stderr, "[FLX4PATCH] IN devcaps wMid/wPid injected dev_id=%u\n", dev_id);
+    }
+
     return MMSYSERR_NOERROR;
 }
 
@@ -1385,6 +1399,21 @@ static DWORD midi_in_reset(WORD dev_id, struct notify_context *notify)
     return err;
 }
 
+#ifndef DRV_QUERYDEVICEINTERFACE
+#define DRV_QUERYDEVICEINTERFACE     (0x0800 + 12)  /* 0x80C */
+#define DRV_QUERYDEVICEINTERFACESIZE (0x0800 + 13)  /* 0x80D */
+#endif
+
+/* szPname 안에 "FLX4"가 들어있는지 (unixlib이라 wcsstr 못 씀) */
+static int match_flx4(const WCHAR *s)
+{
+    if (!s) return 0;
+    for (; *s; s++)
+        if (s[0]=='F' && s[1]=='L' && s[2]=='X' && s[3]=='4')
+            return 1;
+    return 0;
+}
+
 NTSTATUS alsa_midi_out_message(void *args)
 {
     struct midi_out_message_params *params = args;
@@ -1435,6 +1464,32 @@ NTSTATUS alsa_midi_out_message(void *args)
     case MODM_RESET:
         *params->err = midi_out_reset(params->dev_id);
         break;
+    case DRV_QUERYDEVICEINTERFACESIZE:
+    case DRV_QUERYDEVICEINTERFACE:
+    {
+        static const WCHAR flx4_if[] =
+            u"\\\\?\\USB#VID_2B73&PID_0045#512&256&1&0#"
+            u"{A5DCBF10-6530-11D2-901F-00C04FB951ED}";
+        DWORD len = 0, need;
+        while (flx4_if[len]) len++;
+        need = (len + 1) * sizeof(WCHAR);
+        if (params->dev_id < num_dests &&
+            match_flx4(dests[params->dev_id].caps.szPname))
+        {
+            if (params->msg == DRV_QUERYDEVICEINTERFACESIZE) {
+                *(DWORD *)params->param_1 = need;
+                *params->err = MMSYSERR_NOERROR;
+                fprintf(stderr, "[FLX4PATCH] OUT SIZE->%u\n", need);
+            } else {
+                memcpy((void *)params->param_1, flx4_if, need);
+                *params->err = MMSYSERR_NOERROR;
+                fprintf(stderr, "[FLX4PATCH] OUT COPY done, first=%04x len=%u p2=%Ix\n",
+                        ((WCHAR*)params->param_1)[0], need, (unsigned long)params->param_2);
+            }
+        } else
+            *params->err = MMSYSERR_NOTSUPPORTED;
+        break;
+    }
     default:
         TRACE("Unsupported message\n");
         *params->err = MMSYSERR_NOTSUPPORTED;
@@ -1490,6 +1545,32 @@ NTSTATUS alsa_midi_in_message(void *args)
     case MIDM_RESET:
         *params->err = midi_in_reset(params->dev_id, params->notify);
         break;
+    case DRV_QUERYDEVICEINTERFACESIZE:
+    case DRV_QUERYDEVICEINTERFACE:
+    {
+        static const WCHAR flx4_if[] =
+            u"\\\\?\\USB#VID_2B73&PID_0045#512&256&1&0#"
+            u"{A5DCBF10-6530-11D2-901F-00C04FB951ED}";
+        DWORD len = 0, need;
+        while (flx4_if[len]) len++;
+        need = (len + 1) * sizeof(WCHAR);
+        if (params->dev_id < num_srcs &&
+            match_flx4(srcs[params->dev_id].caps.szPname))
+        {
+            if (params->msg == DRV_QUERYDEVICEINTERFACESIZE) {
+                *(DWORD *)params->param_1 = need;
+                *params->err = MMSYSERR_NOERROR;
+                fprintf(stderr, "[FLX4PATCH] IN SIZE->%u\n", need);
+            } else {
+                memcpy((void *)params->param_1, flx4_if, need);
+                *params->err = MMSYSERR_NOERROR;
+                fprintf(stderr, "[FLX4PATCH] IN COPY done, first=%04x len=%u p2=%Ix\n",
+                        ((WCHAR*)params->param_1)[0], need, (unsigned long)params->param_2);
+            }
+        } else
+            *params->err = MMSYSERR_NOTSUPPORTED;
+        break;
+    }
     default:
         TRACE("Unsupported message\n");
         *params->err = MMSYSERR_NOTSUPPORTED;
diff --git a/dlls/winmm/winmm.c b/dlls/winmm/winmm.c
index 73050e0..7bc2e6b 100644
--- a/dlls/winmm/winmm.c
+++ b/dlls/winmm/winmm.c
@@ -881,8 +881,12 @@ UINT WINAPI midiInMessage(HMIDIIN hMidiIn, UINT uMessage,
 
     TRACE("(%p, %04X, %08IX, %08IX)\n", hMidiIn, uMessage, dwParam1, dwParam2);
 
-    if ((wmld = MMDRV_Get(hMidiIn, MMDRV_MIDIIN, FALSE)) == NULL)
-	return MMSYSERR_INVALHANDLE;
+    if ((wmld = MMDRV_Get(hMidiIn, MMDRV_MIDIIN, FALSE)) == NULL) {
+        if ((wmld = MMDRV_Get(hMidiIn, MMDRV_MIDIIN, TRUE)) != NULL) {
+            return MMDRV_PhysicalFeatures(wmld, uMessage, dwParam1, dwParam2);
+        }
+        return MMSYSERR_INVALHANDLE;
+    }
 
     switch (uMessage) {
     case MIDM_OPEN:
